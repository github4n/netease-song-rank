<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.olook.netease.song.rank.mapper.TimerJobRecordMapper">

    <select id="getLatestRecord" resultType="me.olook.netease.song.rank.entity.TimerJobRecord">
        SELECT * from song_rank_job_record where job_id = #{jobId} and new_data = '1'
        order by end_time desc
        limit 1
    </select>
</mapper>